{
  "name": "sportify-backend",
  "version": "1.0.0",
  "description": "Backend API for Sportify Angular app with Spotify preview finding",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "build": "echo 'No build step required'",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": ["spotify", "preview", "api", "backend", "express"],
  "author": "Your Name",
  "license": "MIT",
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "spotify-preview-finder": "^1.0.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}
